#include <stdio.h>
int main() {
    int n,i;
     int product=1;
     printf("enter a no:");
     scanf("%d",&n);
     for (i=1;i<=n;i++){product=product*i;}
     printf("factorial of %d is %d\n",n,product);
    return 0;
}
