#include <stdio.h>
#include<math.h>
int main() {
    int num,digit,rev=0;
   printf("enter a no:");
   scanf("%d",&num);
   while(num!=0){
       digit=num%10;
       rev=rev*10+digit;
       num=num/10;
   }
   printf("%d",rev);
    return 0;
}
